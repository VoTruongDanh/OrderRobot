# AI Memory

## Project Snapshot

- Repo: `OrderRobot`
- Main areas:
  - `apps/kiosk-ui`: kiosk frontend + admin UI
  - `services/ai-backend`: conversation, STT/TTS, bridge logic
  - `services/core-backend`: menu/order/POS integration
- Current focus is kiosk ordering flow in Vietnamese, with voice-first UX.

## Current Runtime Assumptions

- Kiosk uses GLB-only robot mode. Old vector/sketchfab paths should not be reintroduced.
- Voice flow is expected to work end-to-end on kiosk:
  - add item by speech
  - review cart
  - confirm order
  - show QR/payment flow
- Admin changes for voice/listen mode should apply immediately to kiosk.
- Tax is configurable from admin and currently defaults to `10%`.

## Known Bugs & Fixes

### Bug: Minus button doesn't decrease quantity (FIXED)
**Symptom:** Clicking minus (-) button in cart sends request to backend but quantity doesn't decrease.

**Root Cause:** 
Frontend sends `"bỏ 1 [item name] size [size]"` to backend, but backend session cart may be empty (after restart or session timeout), causing the remove command to fail silently.

**Fix Applied:**
Changed minus button to update local cart directly without calling backend:
- Decrease quantity by 1 in local cart state
- Remove item if quantity becomes 0
- Update UI immediately
- Backend will be synced during checkout via rehydration logic

**Files Touched:**
- `apps/kiosk-ui/public/stitch_robot_3d_control_center.html` - Modified cart minus button handler
- `services/ai-backend/app/services/conversation_engine.py` - Added debug logging for remove operations

**Benefits:**
- Instant UI feedback (no network delay)
- No dependency on backend session state
- Consistent with optimistic update pattern used by plus button
- Backend sync happens automatically during checkout

### Bug: Second confirm doesn't show QR (FIXED)
**Symptom:** On some machines, clicking "Xác nhận lần 2" doesn't show QR code payment popup.

**Root Cause:** 
Core backend was using empty CSV menu instead of remote POS API when creating orders, even though `POS_MENU_SOURCE_MODE=remote_strict` was configured.

The issue was in `create_order()` logic:
```python
# OLD (BROKEN):
remote_live_mode = self.remote_pos_enabled and self.remote_menu_strict_enabled
if remote_live_mode:
    menu_items = self._get_remote_menu_items_by_ids(...)  # Remote API
else:
    menu_items = self.menu_repository.get_items_by_ids(...)  # CSV (empty!)
```

When `remote_pos_enabled=False` (e.g., auth not ready), it fell back to CSV even though `remote_menu_strict_enabled=True`.

**Fix Applied:**
Separated menu validation from order creation:
- `use_remote_menu` = `remote_menu_strict_enabled` (for menu validation)
- `remote_live_mode` = `remote_pos_enabled AND remote_menu_strict_enabled` (for POS order creation)

Now when `POS_MENU_SOURCE_MODE=remote_strict`, menu validation ALWAYS uses remote API, never CSV.

**Files Touched:**
- `services/core-backend/app/services/order_service.py` - Fixed create_order logic
- `services/core-backend/app/main.py` - Added logging
- `services/ai-backend/app/services/conversation_engine.py` - Added error handling

**Verification:**
- Check logs: `use_remote_menu=True` when `POS_MENU_SOURCE_MODE=remote_strict`
- Order creation should succeed with remote menu items
- QR popup should appear after second confirm

**Remaining Risks:**
- If remote API is down, order creation will fail (no CSV fallback by design)
- Need to handle remote API errors gracefully

## Important Frontend Rules

- File most frequently touched: `apps/kiosk-ui/public/stitch_robot_3d_control_center.html`
- Cart merging must be size-aware:
  - never merge by bare `item_id`
  - use a size-aware key such as `item_key = item_id + size`
- Manual UI add flow and voice optimistic add flow both must preserve separate cart lines per size.
- Do not trust backend cart sync blindly for manual/optimistic flows:
  - empty payload carts must not wipe a valid local optimistic cart
  - backend item-id-only carts can collapse different sizes if applied directly
- Exception: manual size selection coming from the voice `clarify_size` modal must allow backend cart sync, otherwise tapping a size can succeed on backend but appear unchanged on frontend.
- Voice checkout behavior:
  - first confirm opens local order review popup
  - second confirm submits actual checkout
- When backend asks `clarify_size`, kiosk should also open the in-app size picker so users can tap size manually instead of relying only on STT.
- For voice `clarify_size`, tapping a size should use the frontend manual-add path with explicit `itemId + size + unitPrice` so the cart updates immediately even if backend pending-size context is flaky.
- Review popup and mini cart are portrait-friendly and part of the expected UX.
- Size selection uses in-app modal, not `window.prompt`.
- Out-of-stock sizes must stay disabled in picker and clearly labeled.
- Chat mini panel should keep the full conversation history scrollable, but visually show about 3 recent chat blocks by default.
- Order invoice popup should support temporary hide/reopen without breaking the active order flow, and its auto-close window is now 3 minutes.

## Important Backend Rules

- File most frequently touched: `services/ai-backend/app/services/conversation_engine.py`
- Conversation should favor kiosk ordering UX over generic fallback:
  - short negative replies like `khong` after add/size flow should continue order flow, not drop to fallback
  - common STT mishearing aliases for `size` should still resolve correctly
- Local review popup does not automatically mean backend is awaiting confirmation:
  - second-step checkout from kiosk must send an explicit `quick_checkout` signal
  - otherwise backend can interpret the second confirm as only the first backend confirmation step
- Broken Vietnamese spacing / no-tone bridge text should be repaired or replaced with safe accented replies before showing on frontend.
- Single clear item match with ordering intent can auto-add even if confidence is not very high.
- Stream responses must be safe against invalid Unicode surrogate data.

## TTS / STT Notes

- TTS engine is VieNeu.
- Important compatibility lesson:
  - old VieNeu runtime + Turbo model caused slow/noisy output
  - safe fallback path was added for older runtime
- Frontend WS playback must respect backend `sample_rate`, not assume fixed `24k`.
- Current `.env.example` still shows:
  - `TTS_ENGINE=vieneu`
  - `VOICE_LISTEN_MODE=always`
  - `VOICE_ALWAYS_LISTEN=true`
  - `TTS_STREAM_PLAYBACK_RATE=1.15`
  - `CHAT_CLEAR_ON_ORDER_COMPLETE=false`

## Admin UI Notes

- `apps/kiosk-ui/src/AdminPage.tsx` was forced to English-only by making `t()` always return the English string.
- If bilingual admin is needed again later, restore the language-aware `t()` behavior instead of mass-editing strings again.
- Listen Mode in admin was changed to instant-apply behavior.
- `apps/kiosk-ui/src/main.tsx` now gates `/admin` with an English-only sign-in card while kiosk route remains open.
- Login UX pattern to keep: concise title + one-line context, high-contrast CTA, clear loading/error states, and mobile-safe spacing.
- Accessibility pattern to keep: `role="alert"` for auth errors, explicit field labels, and visible focus rings on inputs/buttons.
- Admin sign-in now retries via `core-backend /auth/login/proxy` when direct POS auth call is blocked by browser CORS/preflight.

## Regressions To Avoid

- Same product in different sizes collapsing into one cart line.
- Voice confirm staying in chat instead of opening the local review popup.
- Empty backend cart payload wiping the local mini cart after manual add.
- English item names being degraded by Vietnamese alias replacement and failing explicit match.
- Garbled Vietnamese without diacritics appearing in kiosk reply text.
- Turbo VieNeu config causing abnormally slow or noisy speech on old runtime.

## Fast Verification Checklist

- Frontend:
  - `npm --prefix apps/kiosk-ui run build`
- AI backend:
  - `python -m compileall services/ai-backend/app`
- Desktop packaging:
  - `node --check desktop/main.mjs`
  - `powershell -File scripts/build-core-backend.ps1`
  - `powershell -File scripts/build-ai-backend.ps1`
  - `powershell -File scripts/build-desktop.ps1`
- Manual smoke checks:
  - add same drink in size `S` and `L` -> must show 2 cart rows
  - voice confirm with existing cart -> opens review popup
  - second confirm -> creates order / QR flow
  - out-of-stock size -> disabled in picker

## Memory Style Going Forward

- Keep this file as concise state, not a full round-by-round changelog.
- Only record:
  - active architecture decisions
  - non-obvious regressions
  - current runtime assumptions
  - high-value verification commands

## 2026-04-16 Repo Understanding Snapshot

- Repo hiện là kiến trúc 3 phần:
  - `apps/kiosk-ui`: React shell/admin, nhưng kiosk runtime chính vẫn nằm nhiều trong `public/stitch_robot_3d_control_center.html`
  - `services/core-backend`: FastAPI quản lý menu/order, có repository abstraction CSV và cầu nối POS
  - `services/ai-backend`: FastAPI xử lý hội thoại, STT/TTS, stream turn, rồi gọi `core-backend` qua HTTP
- Luồng chính:
  - UI gửi turn stream lên `ai-backend`
  - `ai-backend` giữ state hội thoại/cart, xử lý `clarify_size` và `quick_checkout`
  - Khi cần tạo đơn, `ai-backend` gọi `core-backend /orders`
  - `core-backend` validate menu/size rồi lưu CSV hoặc tạo order sang POS + QR
- Admin route là `/admin`, có login gate và fallback qua `core-backend /auth/login/proxy` để tránh CORS khi dev local.
- Trạng thái repo thực tế không hoàn toàn khớp README:
  - `package.json` và README nhắc `desktop/main.mjs`
  - nhưng workspace hiện tại không có thư mục `desktop/`
  - vì vậy các lệnh desktop chỉ có thể coi là phụ thuộc vào phần source chưa hiện diện hoặc đang thiếu trong snapshot hiện tại.

## Desktop Packaging Notes

- Windows installer path now exists via `Electron + electron-builder`.
- Desktop shell in `desktop/main.mjs` is responsible for:
  - showing a POS login gate before kiosk startup
  - seeding writable runtime files into `AppData`
  - spawning `core`, `ai`, and bridge browser automatically
  - serving built UI locally and loading it in Electron
- Desktop login uses `POS_AUTH_LOGIN_URL` and only boots the app after a valid `accessToken` is returned.
- Backend config now supports `ORDERROBOT_ROOT_DIR` override so packaged app can read/write `.env` and CSVs outside `Program Files`.
- Bridge runtime now supports `BRIDGE_PROFILE_DIR` so hidden browser state is also kept in writable runtime storage.
- Packaging scripts:
  - `scripts/build-core-backend.ps1`
  - `scripts/build-ai-backend.ps1`
  - `scripts/build-desktop.ps1`
- Current machine was able to build `OrderRobotCoreBackend.exe`.
- `OrderRobotAiBackend.exe` packaging reached the final PyInstaller EXE step but failed due local disk space exhaustion, not due code syntax.

## 2026-04-16 Config Hardcode Audit

- `POS_MENU_SOURCE_URL` no longer depends on a fixed `storeId`; backend now syncs it from `POS_STORE_ID` and supports the template form `storeId={storeId}`.
- Admin config persistence now auto-syncs derived values to reduce drift:
  - `POS_AUTH_LOGIN_URL` / `POS_AUTH_REFRESH_URL` from `POS_API_BASE_URL`
  - `VITE_MENU_API_URL` / `VITE_ORDERS_API_URL` from `VITE_CORE_API_URL`
  - `VITE_PRODUCT_SIZE_API_URL` from `POS_SIZE_SOURCE_URL`
  - `VITE_PRODUCT_DEFAULT_SIZE_NAME` from `POS_DEFAULT_SIZE_NAME`
- `services/ai-backend/app/config.py` default `CORE_BACKEND_URL` was corrected from old `8001` fallback to `8011` to match the current runtime.
- Remaining hardcoded values are mostly acceptable defaults/fallbacks for local dev or speech runtime tuning, not primary sources of truth.

## 2026-04-16 Dev AI Port Regression

- `8012` is the default local AI backend port; `18012` is the fallback when `8012` is occupied or incompatible.
- Root cause of repeated `ECONNREFUSED 127.0.0.1:8012` in Vite/Admin was browser-side hardcoded fallback logic:
  - `apps/kiosk-ui/src/config.ts` returned `http://127.0.0.1:8012` in local dev
  - `apps/kiosk-ui/src/AdminPage.tsx` retried direct `8012/18012` URLs
  - `apps/kiosk-ui/public/stitch_robot_3d_control_center.html` also used direct `8012`
- When `dev-ai-lite.ps1` moved AI to `18012`, the browser still called `8012`, so Vite proxy could not help.
- Fix applied:
  - browser-side AI fallback is now `/api/ai`
  - local-loopback AI URLs are normalized back to `/api/ai`
  - Admin AI candidate list no longer probes direct `8012/18012`
- Verification:
  - `node -e "import('./apps/kiosk-ui/vite.config.ts')..."` passed
  - `npm --prefix apps/kiosk-ui run build` passed

## 2026-04-16 Dev Runtime Port Stabilization

- Another root cause for local dev proxy failures was stale runtime port metadata:
  - `data/dev-runtime/ai-port.txt` could stay at `8012` even when the AI backend had moved or should move to `18012`
  - `scripts/dev-ui.ps1` previously timed out after 20s and still launched Vite, even if AI/Core were not yet healthy
  - old `scripts/dev-ai-lite.ps1` had simpler port handling than `scripts/dev-ai.ps1`
- Fix applied:
  - upgraded `scripts/dev-ai-lite.ps1` to use the same robust listener detection / stale-process restart / multi-port fallback flow as realtime `dev-ai.ps1`
  - `scripts/dev-ui.ps1` now resolves healthy Core/AI ports from runtime file plus fallback candidates, rewrites stale port files, and waits up to 60s before starting Vite
  - both `scripts/dev-ai-lite.ps1` and `scripts/dev-ai.ps1` now stop the whole listener process tree (`taskkill /T /F`) instead of only one PID, which reduces stale uvicorn reloaders lingering on `8012`
  - PowerShell bug fixed: do not assign to `$pid` because it collides with read-only automatic variable `$PID`; use `$listenerPid` instead in `dev-ai-lite.ps1`, `dev-ai.ps1`, and `dev-core.ps1`
- Remaining risk:
  - if AI backend truly fails to boot at every candidate port, Vite may still start in degraded mode after timeout, but the warning now points to backend health instead of silently trusting stale `ai-port.txt`

## 2026-04-16 Admin `.env` Persist Failure

- Symptom: Admin showed `Index synced immediately, but failed to persist .env: Failed to fetch` even though local AI/Core ports were healthy.
- Root cause: Vite dev proxy routed `/api/ai/*` and `/api/core/*` without stripping the prefix, unlike nginx/docker which already strips `/api/ai/` and `/api/core/`.
- Result:
  - browser request `/api/ai/config/env/sync` reached backend as `/api/ai/config/env/sync` instead of `/config/env/sync`
  - first candidate failed, then Admin retried `127.0.0.1:8080`, which caused the final visible `Failed to fetch`
- Fix applied:
  - added `rewrite` for `/api/ai` and `/api/core` in `apps/kiosk-ui/vite.config.ts`
  - this keeps dev proxy behavior aligned with `deploy/nginx/default.conf`

## 2026-04-16 Menu Verification Snapshot

- Current local `.env` has:
  - `POS_STORE_ID=10`
  - `POS_MENU_SOURCE_MODE=remote_strict`
  - `POS_MENU_SOURCE_URL=...storeId=10...`
- Local `http://127.0.0.1:8011/menu` returned 14 items, which confirms the running core backend is serving live remote menu data for the configured store instead of CSV fallback.
- Direct shell request to `http://cnxvn.ddns.net:8080/api/v1/product-availability/filter?storeId=10...` failed with `Unable to connect to the remote server`, so independent upstream count verification was not possible from the shell at that moment.

## 2026-04-16 Menu Fallback Guard + Admin Logout

- User reported that switching stores could leave the kiosk showing exactly one bogus item instead of failing clearly.
- Root cause:
  - backend remote menu normalization allowed synthetic fallback ids like `remote-item-{index}`
  - frontend menu normalization allowed synthetic fallback ids like `item-{index}`
  - if upstream payload/schema degraded, the app could render garbage instead of rejecting the payload
- Fix applied:
  - `services/core-backend/app/services/order_service.py` now rejects remote menu rows that do not contain a real numeric product/item id
  - `apps/kiosk-ui/public/stitch_robot_3d_control_center.html` now rejects menu rows without a real numeric id and surfaces a clear error instead of rendering fallback items
  - admin now has a visible logout button wired through `apps/kiosk-ui/src/main.tsx` -> `apps/kiosk-ui/src/AdminPage.tsx`

## 2026-04-16 Auto Reload Menu After Store Change

- Even after `.env` and core runtime were updated to the new `POS_STORE_ID`, kiosk could keep showing the previous store menu because `stitch_robot_3d_control_center.html` cached `menuLoaded/currentMenuItems` and did not invalidate them on admin config changes.
- Fix applied:
  - kiosk now computes a menu config signature from admin env fields
  - on `storage`, `focus`, and `visibilitychange`, it invalidates menu cache when the signature changes
  - it then auto-runs `ensureMenuLoaded()` so the new store menu appears without restarting the server
